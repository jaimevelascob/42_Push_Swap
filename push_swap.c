/* "1. ejecutar ./push_swap 1 2 3 4 5 y printear los numeros " */
#include "stdio.h"
#include "push_swap.h"
#include "stdlib.h"

int push(int d)
{
    // check if is empty
    // 

}

int main(int argc, char **argv)
{
    while(argc-- > 1)
    {
        /* if is number -- add to a */
        if (atoi(argv[argc]))
		printf("hola");
        else
        {
            printf("error");
            return 0; 
        }
    }
}
